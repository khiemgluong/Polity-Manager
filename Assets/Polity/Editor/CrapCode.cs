using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CrapCode : MonoBehaviour
{
    //Bad code that I havent decided to delete yet

    //  private void AddNode()
    //     {
    //         Debug.Log("Attempting to add node...");
    //         if (polityMemberProband != null && PrefabUtility.IsPartOfPrefabAsset(polityMemberProband))
    //         {
    //             polityMemberPrefab = polityMemberProband;
    //             Debug.Log("Adding node...");
    //             SerializedObject serializedPolityMember = new SerializedObject(polityMemberPrefab);
    //             SerializedProperty familyProperty = serializedPolityMember.FindProperty("family");

    //             nodes.Add(serializedPolityMember);
    //             nodeFamilyProperties.Add(familyProperty);

    //             Rect lastRect = new Rect(10, 10, nodeWidth, nodeHeight);
    //             if (nodeRects.Count > 0)
    //             {
    //                 lastRect = nodeRects[nodeRects.Count - 1];
    //                 lastRect.x += nodeWidth + nodeSpacing;
    //             }
    //             nodeRects.Add(lastRect);
    //             Debug.Log("Node added at position: " + lastRect);
    //         }
    //         else
    //         {
    //             Debug.Log("PolityMemberPrefab is null or not a prefab asset");
    //         }
    //     }
}
